// Display an alert box
window.alert('Hello omda');

// Get the user's browser information
console.log(navigator.userAgent);

// Get the screen width and height
console.log('Screen width: ' + screen.width);
console.log('Screen height: ' + screen.height);

// Redirect to another URL
window.location.href = 'https://www.example.com';

// Navigate back in history
window.history.back();
//    window: The global object representing the browser window. Methods include alert(), confirm(), setTimeout(), setInterval().
//navigator: Contains information about the browser, such as navigator.userAgent.
//screen: Provides information about the user's screen, such as screen.width and screen.height.
//location: Represents the URL of the current document. Methods include location.href, location.reload().
//history: Provides access to the browser's session history. Methods include history.back(), history.forward(), history.go().